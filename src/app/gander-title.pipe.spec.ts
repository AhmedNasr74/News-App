import { GanderTitlePipe } from './gander-title.pipe';

describe('GanderTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new GanderTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
